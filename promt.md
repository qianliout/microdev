你是一个高级golang开发工程师，请创建一个完整的Golang命令行工具，实现以下功能：

## 核心功能
开发一个名为`outback`的命令行工具，支持子命令结构，当前实现`p`子命令：
```
./outback p --in=INPUT_CONTENT [--out=OUTPUT_FILE]
```

## 详细需求
1. **输入处理**：
   - `--in`参数接受文件路径或直接文本
   - 如果是有效文件路径，读取文件内容
   - 如果不是有效文件，直接使用输入字符串

2. **输出处理**：
   - `--out`参数指定输出文件路径（相对路径基于当前工作目录）
   - 文件不存在时创建，存在时追加内容
   - 未指定`--out`时，输出到命令行（仅输出模型生成内容）

3. **LLM集成**：
   - 使用langchaingo库(https://github.com/tmc/langchaingo)调用通义千问模型`qwen-plus-latest`
   - 生成中文优化的prompt内容
   - 实现流式输出（命令行实时显示，文件写入时也保持流式）
   - 尽量使用开源库,减少手动造轮子

4. **环境配置**：
   - 从环境变量读取`DASHSCOPE_API_KEY`和`ALI_BAILIAN_API_KEY`
   - 支持阿里云百炼平台认证

## 技术规范
1. **代码结构**：
   - 模块化设计，易于扩展新子命令
   - 清晰的包分层：cmd, pkg, component 等等
   - 完整的错误处理和日志系统

2. **测试要求**：
   - 在`tests/`目录下编写完整测试套件
   - 包含单元测试和集成测试
   - 确保所有测试通过

3. **文档要求**：
   - 提供详细的使用说明文档
   - 包含安装、配置、使用示例
   - 代码注释完整清晰

## 质量要求
- 符合Go语言最佳实践
- 代码可维护性强，便于后续功能扩展
- 性能优化，正确处理并发和资源管理
- 完整的错误处理和用户友好提示
- 命令行可以运行成功 

## 交付物
- 完整的Golang项目源码
- 通过所有测试的验证代码
- 详细的使用文档和API说明
- 配置示例和环境设置指南

请生成可直接编译运行的高质量代码，确保功能完整性和代码健壮性。




